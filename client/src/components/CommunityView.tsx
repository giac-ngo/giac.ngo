// CommunityView: placeholder — Community tab is handled by PracticeSpacePage social feed
import React from 'react';

export const CommunityView: React.FC = () => {
  return (
    <div style={{ padding: '2rem', textAlign: 'center', color: 'var(--color-text-light)' }}>
      {/* Community feed is rendered directly in PracticeSpacePage */}
    </div>
  );
};

export default CommunityView;
